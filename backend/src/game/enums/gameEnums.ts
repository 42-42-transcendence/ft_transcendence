export enum PaddlePos{
    LeftFront,
    LeftUp,
    LeftDown,
    RightFront,
    RightUp,
    RightDown
}

export enum CanvasPosition {
    Top,
    Bottom,
    Right,
    Left,
}

export enum BallCorner {
    TopRight,
    BottomRight,
    TopLeft,
    BottomLeft
}

export enum ObjectCorner {
    TopRight,
    BottomRight,
    TopLeft,
    BottomLeft
}