import { useEffect } from 'react';
import Channel from '../components/Channel';

const ChannelsPage = () => {
  useEffect(() => {}, []);

  return (
    <>
      <h1>Channels Page</h1>
      <Channel />
    </>
  );
};

export default ChannelsPage;
